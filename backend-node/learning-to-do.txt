check the dependencies of express module
waht are mime - types
caret and tilda

"test": "echo \"Error: no test specified\" && exit 1"

minimum string pattern search for URL -> order of api path is important in node

Regular expressions in nodejs routes: ?,*, (), +

    use of regex in routes / a / /.*fly$/
Reading the express documentations: query params and dynamic routes in nodejs
elamanchili ammagaru 
8367475200-
9849209622- ayurvedic doctor 

read more about middleware and why do we need middlewares.
app.use vs app.all